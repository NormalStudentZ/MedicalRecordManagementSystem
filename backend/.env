# 安全警告：不要将此文件提交到版本控制！
# 将此文件添加到.gitignore

# 应用基础配置
FLASK_ENV=development                      # Flask运行环境模式，development为开发模式
SECRET_KEY=grarorajgajajjgrnanlw          # Flask应用密钥，用于会话加密和CSRF保护

# 开发环境数据库配置
DB_HOST=127.0.0.1                         # 数据库服务器地址（本地地址）
DB_PORT=5432                              # 数据库端口号（PostgreSQL默认端口）
DB_USER=postgres                          # 数据库用户名
DB_PASSWORD=hhh114514                     # 数据库密码
DB_NAME=CaseManagementSystem              # 数据库名称

# ========== 连接池详细配置 ==========
# 基础连接池配置
POOL_SIZE=10                             # 核心连接数，建议10-20
MAX_OVERFLOW=20                          # 最大溢出连接数，建议为核心数的2倍
POOL_RECYCLE=3600                        # 连接回收时间（秒），建议3600（1小时）
POOL_PRE_PING=True                       # 每次获取连接前检测是否可用，推荐True
POOL_TIMEOUT=30                          # 获取连接超时时间（秒）

# 高级配置（可选）
ECHO_POOL=False                          # 是否打印连接池日志
SQL_ECHO=False                           # 是否打印SQL语句
ENABLE_POOL_MONITOR=False                # 是否启用连接池监控（生产环境建议False）

# ========== 生产环境数据库配置 ==========
# PROD_DB_HOST=your-production-db-host.com
# PROD_DB_PORT=5432
# PROD_DB_USER=prod_user
# PROD_DB_PASSWORD=prod_password
# PROD_DB_NAME=prod_db_name

# 生产环境连接池配置（更保守）
PROD_POOL_SIZE=20                        # 生产环境核心连接数
PROD_MAX_OVERFLOW=30                     # 生产环境最大溢出连接数
PROD_POOL_RECYCLE=1800                  # 生产环境连接回收时间（30分钟）
PROD_POOL_TIMEOUT=20                    # 生产环境获取连接超时时间
PROD_CONNECT_TIMEOUT=10                # 生产环境连接超时时间

# 邮箱配置
MAIL_SERVER = "smtp.qq.com"               # 邮件服务器地址（QQ邮箱SMTP服务器）
MAIL_USE_TLS = False                      # 是否使用TLS加密（QQ邮箱使用SSL）
MAIL_USE_SSL = True                       # 是否使用SSL加密
MAIL_PORT = 465                           # 邮件服务器端口（SSL端口）
MAIL_USERNAME = "3439458467@qq.com"       # 发件人邮箱地址
MAIL_PASSWORD = "zznlilfnjucrdbhh"        # 邮箱授权码（不是邮箱密码）
MAIL_DEFAULT_SENDER = "3439458467@qq.com" # 默认发件人地址
